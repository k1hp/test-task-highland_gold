[project]
name = "test-tasks-highland-gold"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=2.3.3",
    "pydantic>=2.12.5",
    "pyqt5>=5.15.11",
    "pytest>=9.0.2",
]

[tool.uv]

required-environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
    "sys_platform == 'win32' and platform_machine == 'AMD64'",
    # Add other required environments as needed (e.g., macOS, different architectures)
]
