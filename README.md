# Проект для тестирования QGIS приложения с геообработкой данных.


## Запуск

1. Для запуска тестов вам потребуется docker
2. Команды для запуска:
```
docker build -t qgis-test ./src
```
```
docker run --rm qgis-test
```